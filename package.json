{
  "name": "paystub-calculator-web",
  "private": true,
  "description": "Static payroll calculator — no bundler.",
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "pages:deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.82.0"
  }
}
